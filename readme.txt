1. Install the libraries: opencv-python, opencv-contrib-python, pillow, numpy
2. In the images folder, create a directory for each person with their name, which you want to be recognized
3. Run the faces_train.py file
4.Run the main.py file